# Devices

`src/devices/` contains the BREAD-facing adapter code.

Planned structure:

- `common/` for small shared helpers only
- `rlht/` for RLHT-specific adapter code
- `dcmt/` for DCMT-specific adapter code
