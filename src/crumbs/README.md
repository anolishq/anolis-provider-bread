# CRUMBS Session Layer

`src/crumbs/` is reserved for Linux host and CRUMBS session code: bus access, scan or read or write primitives, timing policy, and diagnostics.

BREAD semantics should stay above this layer.
